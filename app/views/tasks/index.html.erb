<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>

    <li><%= link_to task.title, task_path(task.id)%></li>

  <% end %>
</ul>

 <li><%= link_to 'Create Task', tasks_new_path%></li>


<%# <%= link_to task.title, task_path(task.id)%>
